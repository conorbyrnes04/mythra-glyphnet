# glyph-dream-db

A lightweight, JSON-backed database for managing **generic glyphs** (archetypal symbols) and **dream glyphs** (unique, per-dream visualizations), with automatic user-centric frequency tracking.

---

## 🚀 Features

- **gGlyphs**: store titled SVG symbols (fire, water, etc.)  
- **dGlyphs**: store dream entries with:
  - unique dream SVG (`dGlyph`)
  - raw transcript, emotion data, context (geolocation, moon phase, weather)
  - interpretive fields (coreThemes, symbolicMeanings, interpretation)
  - weighted counts of generic glyph co-occurrences  
- **Users**: track which dreams they’ve had, plus frequency maps of gGlyphs & emotions  
- **CLI & API**:  
  - Interactive CLI for seeding and listing glyphs  
  - REST API (Express) for full CRUD on both gGlyphs and dGlyphs  
- **File-based**: everything lives in `data/*.json`—no external database required.

---

## 📦 Installation

```bash
git clone git@github.com:conorbyrnes04/matkra-glyph-dream-db.git
cd matrka-glyph-dream-db
npm install
npm install lowdb

